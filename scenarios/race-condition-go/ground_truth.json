{
  "expected_findings": [
    {
      "category": "correctness",
      "severity": "critical",
      "file": "internal/cache/sessions.go",
      "line_range": [15, 46],
      "description": "SessionStore uses bare maps without mutex protection, causing data races under concurrent access",
      "keywords": ["race condition", "concurrent", "map", "mutex", "sync", "data race"],
      "consolidated_with": []
    },
    {
      "category": "correctness",
      "severity": "critical",
      "file": "internal/cache/ratelimit.go",
      "line_range": [10, 37],
      "description": "RateLimiter uses bare maps without mutex protection, causing data races under concurrent access",
      "keywords": ["race condition", "concurrent", "map", "mutex", "sync", "data race"],
      "consolidated_with": []
    },
    {
      "category": "resource",
      "severity": "high",
      "file": "internal/cache/ratelimit.go",
      "line_range": [24, 36],
      "description": "Expired rate-limit windows are never evicted, causing unbounded memory growth under high-cardinality keys",
      "keywords": ["memory leak", "eviction", "cleanup", "unbounded growth", "map"],
      "consolidated_with": []
    }
  ],
  "expected_clean": false,
  "max_acceptable_findings": 5,
  "metadata": {
    "language": "go",
    "difficulty": "medium",
    "description": "Cache components with race conditions from unprotected concurrent map access"
  }
}
